{
  "name": "chog-casino-operator",
  "version": "0.0.0",
  "private": true,
  "description": "Off-chain commit-reveal operator service for the Chog Casino contracts in ../contracts. Generates server seeds, commits their hash, shows live card values during Blackjack play, and reveals to settle bets.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "verify-math": "tsx src/verifyCardMath.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "viem": "^2.52.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.3.3",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3"
  }
}
